server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:h2:mem:rumldb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
